% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell-type-specific_net.R
\name{specific_cluster}
\alias{specific_cluster}
\title{Identify Specific Clusters from Correlation Matrix}
\usage{
specific_cluster(cor, minClusterSize = 10)
}
\arguments{
\item{cor}{A correlation matrix (square numeric matrix or data frame, 
rownames and colnames should match).}

\item{minClusterSize}{Minimum cluster size (default = 10).}
}
\value{
A data frame with one column:
\item{cluster}{Cluster assignment for each variable (columns of \code{cor}).}
Row names correspond to variable names.
}
\description{
This function applies hierarchical clustering and dynamic tree cutting 
(via \pkg{WGCNA} and \pkg{dynamicTreeCut}) to identify clusters from a correlation matrix.
}
\examples{
\dontrun{
cor_mat <- cor(matrix(rnorm(1000), nrow = 50))
clusters <- specific_cluster(cor_mat, minClusterSize = 5)
head(clusters)
}
}
